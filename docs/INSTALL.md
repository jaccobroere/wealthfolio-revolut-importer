# Installation

Download the versioned ZIP and `SHA256SUMS`, run `shasum -a 256 -c
SHA256SUMS`, and install the verified ZIP from Wealthfolio’s add-on settings.
Review symbols and mappings before importing.
