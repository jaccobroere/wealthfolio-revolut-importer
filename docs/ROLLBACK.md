# Rollback

Verify a previously published ZIP against its immutable `SHA256SUMS`, then
install it through Wealthfolio’s add-on installer. Do not retag or overwrite a
release. Re-import remains fingerprint-safe.
