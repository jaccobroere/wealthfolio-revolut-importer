# Wealthfolio SDK contract

This addon targets Wealthfolio 3.6.1 and uses account enumeration, scoped
import mappings, market symbol search, activity search, and bulk writes. Host
dependencies are externalized according to `manifest.json`; parser
dependencies are bundled. The package validator enforces archive limits and
runtime asset closure.
